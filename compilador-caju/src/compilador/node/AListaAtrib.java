/* This file was generated by SableCC (http://www.sablecc.org/). */

package compilador.node;

import java.util.*;
import compilador.analysis.*;

@SuppressWarnings("nls")
public final class AListaAtrib extends PListaAtrib
{
    private PAtrib _atrib_;
    private final LinkedList<PMultiplosAtributos> _multiplosAtributos_ = new LinkedList<PMultiplosAtributos>();

    public AListaAtrib()
    {
        // Constructor
    }

    public AListaAtrib(
        @SuppressWarnings("hiding") PAtrib _atrib_,
        @SuppressWarnings("hiding") List<?> _multiplosAtributos_)
    {
        // Constructor
        setAtrib(_atrib_);

        setMultiplosAtributos(_multiplosAtributos_);

    }

    @Override
    public Object clone()
    {
        return new AListaAtrib(
            cloneNode(this._atrib_),
            cloneList(this._multiplosAtributos_));
    }

    @Override
    public void apply(Switch sw)
    {
        ((Analysis) sw).caseAListaAtrib(this);
    }

    public PAtrib getAtrib()
    {
        return this._atrib_;
    }

    public void setAtrib(PAtrib node)
    {
        if(this._atrib_ != null)
        {
            this._atrib_.parent(null);
        }

        if(node != null)
        {
            if(node.parent() != null)
            {
                node.parent().removeChild(node);
            }

            node.parent(this);
        }

        this._atrib_ = node;
    }

    public LinkedList<PMultiplosAtributos> getMultiplosAtributos()
    {
        return this._multiplosAtributos_;
    }

    public void setMultiplosAtributos(List<?> list)
    {
        for(PMultiplosAtributos e : this._multiplosAtributos_)
        {
            e.parent(null);
        }
        this._multiplosAtributos_.clear();

        for(Object obj_e : list)
        {
            PMultiplosAtributos e = (PMultiplosAtributos) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._multiplosAtributos_.add(e);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._atrib_)
            + toString(this._multiplosAtributos_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._atrib_ == child)
        {
            this._atrib_ = null;
            return;
        }

        if(this._multiplosAtributos_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        if(this._atrib_ == oldChild)
        {
            setAtrib((PAtrib) newChild);
            return;
        }

        for(ListIterator<PMultiplosAtributos> i = this._multiplosAtributos_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PMultiplosAtributos) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
